
angular
	.module("spinFilters", [])
		# -----------------------------------------------------------------------------
		#
		#    CHECK MARK
		#
		# -----------------------------------------------------------------------------
		.filter "checkmark", ->
			(input) ->
				return if input then '\u2713' else '\u2718'

# EOF
